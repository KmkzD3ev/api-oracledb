CREATE OR REPLACE TRIGGER TRG_ATUALIZAR_DIAS_INSERCAO
BEFORE INSERT ON PRODUTO_VALIDADE
FOR EACH ROW
BEGIN
    -- Atualiza o campo DIAS_PARA_VENCER antes da inserção, considerando apenas as datas
    :NEW.DIAS_PARA_VENCER := TRUNC(:NEW.DT_VALIDADE) - TRUNC(SYSDATE);
END;
/
